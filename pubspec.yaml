name: leader_check
description: Kurz-Check für Leitungsstile im Leiterschaftskurs.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

# Bewusst ohne Fremdpakete: Der gesamte Zustand sind zwanzig Zahlen. Jede
# zusätzliche Abhängigkeit wäre hier Ballast.
dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/items/
